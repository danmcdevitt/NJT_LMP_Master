{
  "name": "njt-lmp-master",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd lmp-site && npm run dev",
    "build": "cd lmp-site && npm install && npm run build",
    "start": "cd lmp-site && npm run start",
    "install": "cd lmp-site && npm install"
  },
  "dependencies": {
    "next": "16.0.10"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
